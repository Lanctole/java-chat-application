package lanctole.message;

public enum MessageType {
    MESSAGE,
    SET_NAME,
    OLD_NAME,
    SYSTEM_NOTIFICATION,
    DISCONNECT,
    REFRESH_CHAT_MEMBERS_LIST,
    NICKNAME_ACCEPTED,
    NICKNAME_DENIED,
    PING
}
